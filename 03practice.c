#include <stdio.h>

int main()
{
    int radious = 3;
    float pi = 3.14;
    float height = 3.23;
    printf("the area of the circle %f \n", pi * radious * radious);

    printf("the volume of the cylinder%f \n", pi * radious * radious * height);

    return 0;
}
// we will took same procedure 02 practice if it was instructed to do through input code
// for finding out the area of the circle and volume of the cyllinder respectively.