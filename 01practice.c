# include <stdio.h>

int main (){
    int length = 2;
    int breadth = 3;
    printf("the area of the rectangle is %d \n",length*breadth);
    return 0;
}
// this is the way to find out area of the rectangle using pre declaration.