# include <stdio.h>

int main (){
    int length,breadth;
    printf("the lenghth of the rectangle is \n");
    scanf("%d",&length);
    printf("the breadth of the rectangle is \n");
    scanf("%d",&breadth);
    printf("the area of the rectangle is %d \n",length*breadth);
    return 0;
} 
// this is the way to find out the area of the rectangle through input program.